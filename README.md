### Website Downloader

To use `npm install`  
Create a file named `sitelist.js` of URL's that looks like this

```
module.exports = {
  websites: [
  "http://example.com/",
  "http://test.com/"
  ]
}
```

Then simply run `node app`